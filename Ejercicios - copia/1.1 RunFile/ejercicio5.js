// Función flecha que toma un string y lo imprime en mayúsculas
const imprimirEnMayusculas = (texto) => {
    console.log(texto.toUpperCase());
};

// Llamamos a la función con un string
imprimirEnMayusculas("i wil be back baby"); // Resultado: HOLA MUNDO